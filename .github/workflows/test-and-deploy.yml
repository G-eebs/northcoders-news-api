name: Test & Deploy
on:
  push:
    branches:
      - main

jobs:
  greeting:
    name: greeting
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hello!"
  test:
    name: Test
    runs-on: ubuntu-latest
    steps: 
      - name: Checkout Repo
        uses: actions/checkout@v3
      - name: Install dependencies
        run: npm ci
      - name: Run utils tests
        run: npm t utils
